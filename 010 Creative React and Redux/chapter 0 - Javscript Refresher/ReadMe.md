```
let and const are pretty much scoped to curly brackets {} 

we can have much control over const and let
```

```
var is only scoped to functions
```

```
Arrays and Objects keep reference in memory
```